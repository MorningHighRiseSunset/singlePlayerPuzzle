# DeepL API key for translated word definitions (server-side only via /api/define)
DEEPL_AUTH_KEY=your-deepl-auth-key:fx

# Pusher credentials for real-time multiplayer
PUSHER_APP_ID=your_app_id
PUSHER_KEY=your_pusher_key
PUSHER_SECRET=your_pusher_secret
PUSHER_CLUSTER=us2
PUSHER_USE_TLS=true
